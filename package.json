{
  "name": "mastersandgods",
  "version": "1.0.0",
  "description": "cool rpg about beating monsters to death with a stick",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "buildFactories": "node scripts/buildFactories.js",
    "rebuild": "npm run buildFactories && npm run build",
    "dev": "node scripts/dev.js",
    "serve": "node scripts/serve.js",
    "site": "npm run build && node scripts/buildSite.js",
    "preview": "node scripts/serve.js 8080 _site",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node scripts/cleanJs.js",
    "cleanJs": "node scripts/cleanJs.js"
  },
  "engines": {
    "node": ">=18"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
